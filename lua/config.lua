local icons = require('icons')

Global = {
    icons = icons,
    ui = {
        float = {
            border = 'rounded',
            highlight = 'NightflyRed' -- check available by :Telescope highlights
        }
    },
}
